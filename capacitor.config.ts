import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'IonicAngularDashboard',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
